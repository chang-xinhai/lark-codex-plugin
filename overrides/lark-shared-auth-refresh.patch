diff --git a/plugins/lark/skills/lark-shared/SKILL.md b/plugins/lark/skills/lark-shared/SKILL.md
index 5b1c94e..f3e3e9d 100644
--- a/plugins/lark/skills/lark-shared/SKILL.md
+++ b/plugins/lark/skills/lark-shared/SKILL.md
@@ -1,7 +1,7 @@
 ---
 name: lark-shared
 version: 1.0.0
-description: "Use for lark-cli setup/auth tasks: auth login/status/logout, user vs bot identity, business-domain permissions (--domain, including all/docs/drive), missing scopes, revoking authorization, or handling _notice JSON."
+description: "Use for lark-cli setup/auth tasks: first-time app configuration versus existing-app user reauthorization, expired refresh tokens, auth login/status/logout, user vs bot identity, business-domain permissions (--domain, including all/docs/drive), missing scopes, revoking authorization, or handling _notice JSON."
 ---
 
 # lark-cli 共享规则
@@ -10,7 +10,7 @@ description: "Use for lark-cli setup/auth tasks: auth login/status/logout, user
 
 ## 配置初始化
 
-首次使用需运行 `lark-cli config init` 完成应用配置。
+仅当目标 profile 尚未绑定应用配置时，运行 `lark-cli config init`。refresh token 过期不代表应用配置丢失；如果 `lark-cli profile list` 或 `lark-cli --profile <name> config show` 仍显示原 `appId`，**禁止**重新执行 `config init --new`，应走下方“已有应用重新授权”流程。
 
 当你帮用户初始化配置时，使用background方式使用下面的命令发起配置应用流程，启动后读取输出，从中提取授权链接并发给用户。
 
@@ -23,15 +23,59 @@ lark-cli config init --new
 
 ## 认证
 
+### 先区分配置、刷新与重新授权
+
+不要把“创建/配置应用”“自动刷新 token”“用户重新授权”混为一谈。处理认证问题时，先运行：
+
+```bash
+lark-cli profile list
+lark-cli --profile <name> auth status --json --verify
+```
+
+根据结果选择且只选择一条流程：
+
+| 状态 | 含义 | 操作 |
+|---|---|---|
+| profile 不存在，或没有 `appId` / `appSecret` | 首次配置应用 | 运行 `config init --new`；组织应用可能需要管理员审批应用 scopes |
+| `identities.user.verified == true`，包括可成功刷新后的 `needs_refresh` | refresh token 仍有效 | 继续调用 user API，让 CLI 自动刷新；不要重新登录，不要重新配置应用 |
+| profile 和 `appId` 存在，但 user 为 `missing` / `expired`，并提示 refresh token expired | 已有应用的用户 refresh token 过期 | 复用同一 profile 和原应用，只对已批准的最小 scopes 发起 user device-flow 重新授权 |
+| API 返回真正缺少的新 scope | 应用或用户缺少权限 | 只请求错误中列出的最小 scope；如果应用后台尚未批准该 scope，才需要管理员处理 |
+
+#### 已有应用的 refresh token 过期
+
+这是用户重新登录，不是创建新应用。必须保留原 profile 与 `appId`：
+
+1. 从 `auth status` 的 `identities.user.scope` 和当前任务中确定已批准且确实需要的最小 scopes。
+2. 使用同一 profile 发起 split-flow：
+
+   ```bash
+   lark-cli --profile <name> auth login \
+     --scope "<already-approved-scope-1> <already-approved-scope-2>" \
+     --no-wait --json
+   ```
+
+3. 用户确认授权后，使用同一 profile 完成登录：
+
+   ```bash
+   lark-cli --profile <name> auth login --device-code <device_code>
+   ```
+
+4. 再运行 `lark-cli --profile <name> auth status --json --verify`，确认 `identities.user.status == "ready"`、`tokenStatus == "valid"`、`verified == true`，并向用户报告实际 user identity。
+
+**管理员审批边界**：组织管理员批准的是应用可使用的 scopes；组织用户随后授权该已批准应用。refresh token 过期时，请求原来已批准的 scopes 通常只会重新签发 user/refresh token。若完成命令显示“本次新授予 scopes: （空）”，应明确告知用户没有新增权限。
+
+**禁止默认扩大权限**：除非用户明确要求首次获取全部权限并知晓可能触发管理员审批，否则不得用 `--domain all` 恢复过期 token。也不得为了恢复 token 而运行 `config init --new`。`--domain all` 可能把尚未批准的 scopes 带入授权页，导致不必要的管理员申请。
+
 ### 认证任务速查
 
 认证、scope、业务域、登录态、退出登录态、撤销授权问题都走本技能。
 
 | 用户意图 | 首选命令 / 回答 |
 |---|---|
-| 获取全部权限 | `lark-cli auth login --domain all --no-wait --json` |
+| 首次获取全部权限（用户明确要求） | `lark-cli auth login --domain all --no-wait --json`；可能触发管理员审批 |
 | 按业务域授权 | `lark-cli auth login --domain docs --domain drive --no-wait --json`；`--domain` 可重复，也可用逗号分隔 |
-| 指定单个 scope 授权 | `lark-cli auth login --scope "<scope>" --no-wait --json` |
+| 指定一个或多个最小 scope 授权 | `lark-cli auth login --scope "<scope-1> <scope-2>" --no-wait --json` |
+| 已有应用的 refresh token 过期 | 复用原 profile：`lark-cli --profile <name> auth login --scope "<already-approved-scopes>" --no-wait --json`；禁止默认 `--domain all` |
 | 检查当前登录态、是谁登录、token 是否有效 | `lark-cli auth status --json --verify`；回答时引用 `identity`、`verified`、`identities.user.status`、`identities.user.userName`、`identities.user.openId`（用户 open id）、`identities.user.tokenStatus`、`identities.user.scope` |
 | 快速查看当前身份状态 | `lark-cli whoami`；实际生效的那一个身份 |
 | 退出当前机器的用户登录态 | `lark-cli auth logout --json`；`loggedOut:true` 表示注销成功 |
@@ -91,8 +135,8 @@ lark-cli auth login --scope "<missing_scope>"   # 按具体 scope 授权（推
 当你作为 AI agent 需要帮用户完成认证时，优先使用 split-flow，避免在同一轮对话中阻塞等待用户授权：
 
 ```bash
-# 发起授权（立即返回 device_code 和 verification_url）
-lark-cli auth login --scope "calendar:calendar:readonly" --no-wait --json
+# 发起授权（立即返回 device_code 和 verification_url）；多 profile 时必须指定目标 profile
+lark-cli --profile <name> auth login --scope "calendar:calendar:readonly" --no-wait --json
 ```
 
 拿到 `verification_url` 后，将它原样作为本轮最终消息发给用户，并结束本轮/交还控制权。不要在同一轮中展示 URL 后立刻执行 `--device-code` 阻塞轮询；在不透传中间输出的 agent harness 里，这会导致用户永远看不到 URL。
@@ -100,14 +144,14 @@ lark-cli auth login --scope "calendar:calendar:readonly" --no-wait --json
 用户回复已完成授权后，再在后续步骤执行：
 
 ```bash
-lark-cli auth login --device-code <device_code>
+lark-cli --profile <name> auth login --device-code <device_code>
 ```
 
 **Split-Flow 完整步骤**：
 
 **第一步：发起授权（当前轮）**
 
-1. 执行 `lark-cli auth login --scope "xxx" --no-wait --json`（必须加 `--no-wait --json`）
+1. 执行 `lark-cli --profile <name> auth login --scope "xxx" --no-wait --json`（必须加 `--no-wait --json`；若只有默认 profile，可省略 `--profile`）
 2. 从 JSON 输出中提取 `verification_url` 和 `device_code`
 3. 生成二维码：`lark-cli auth qrcode <verification_url> --output "xxx"`
 4. 将 URL 和二维码展示给用户（先 URL，后二维码）
@@ -116,7 +160,7 @@ lark-cli auth login --device-code <device_code>
 **第二步：完成授权（后续轮）**
 
 1. 等待用户回复"已完成授权"
-2. **由你（AI agent）亲自执行**：`lark-cli auth login --device-code <device_code>`
+2. **由你（AI agent）亲自执行**：`lark-cli --profile <name> auth login --device-code <device_code>`；必须与第一步使用同一 profile
 3. 此命令会轮询授权状态并完成登录
 4. 如果返回授权成功，流程结束
 
@@ -125,6 +169,7 @@ lark-cli auth login --device-code <device_code>
 - **你必须亲自执行 `--device-code` 命令**，不要指示用户自行执行
 - **不要在同一轮中展示 URL 后立刻执行 `--device-code`**，这会导致用户看不到 URL
 - **禁止缓存 `verification_url` 或 `device_code`**：每次需要授权时，必须重新执行 `lark-cli auth login --no-wait --json` 生成新的链接。不要将授权链接和 device code 存入上下文供后续复用
+- **不要用 `--domain all` 处理 refresh token 过期**：复用原 profile，并请求当前任务所需的已批准最小 scopes
 
 ## 更新检查
 
